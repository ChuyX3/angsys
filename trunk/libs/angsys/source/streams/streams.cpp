#include "pch.h"
#include <ang/streams.h>

using namespace ang;
using namespace ang::streams;

ANG_ENUM_IMPLEMENT(ang::streams, stream_mode);
ANG_ENUM_IMPLEMENT(ang::streams, special_command);
ANG_ENUM_IMPLEMENT(ang::streams, stream_reference);

